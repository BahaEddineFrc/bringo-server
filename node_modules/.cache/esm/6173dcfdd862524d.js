let routes;_d86‍.w("./config/routes",[["default",["routes"],function(v){routes=v}]]);const express = require('express');
const mongoose = require('mongoose');
const https = require('https');
const bodyParser = require('body-parser');
const fs = require('fs');

//const app = express();

var privateKey = fs.readFileSync('cert/client-key.pem').toString();
var certificate = fs.readFileSync('cert/client-cert.pem').toString();  

//var app = express.createServer({key: privateKey, cert: certificate});



//const hostname = '127.0.0.1';
const port = 3000;

//mongoose.connect('mongodb://127.0.0.1:27017/bringo-server',{ useNewUrlParser: true })
mongoose.connect('mongodb://bringo_db:bringo_db0@ds341247.mlab.com:41247/bringo-server')
.then(() => console.log("Connected to MongoDB..."))
.catch(err => console.error("Could not connect to MongoDB..."));

app.use(bodyParser.urlencoded({ extended: false }));
app.use(bodyParser.json());

app.use(routes)

https.createServer({key: privateKey, cert: certificate}, app).listen(process.env.PORT || port, (err) => {
    if (err) _d86‍.g.console.log(err);
    else  console.log('Server started on port 3000...');
})


/*  sudo service mongod start
    node -r esm app.js
    node app.js        */ 